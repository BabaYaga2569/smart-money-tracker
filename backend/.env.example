# Plaid Configuration
# For development, use sandbox credentials
# Get your credentials from https://dashboard.plaid.com/
PLAID_CLIENT_ID=your_client_id_here
PLAID_SECRET=your_secret_here
PLAID_ENV=sandbox

# Firebase Admin SDK (for secure server-side Firestore access)
# Get this from Firebase Console -> Project Settings -> Service Accounts -> Generate New Private Key
# Set as single-line JSON string (escape quotes if needed)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}

# Server Configuration
PORT=5000
